import * as React from "react"
import { Link } from 'gatsby'

const Greeting = (props) => {
    return (
        
        <h1>Hello, {props.name}! This is your prop.</h1>
               
    )
}

export default Greeting